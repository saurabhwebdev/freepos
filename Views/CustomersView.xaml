<UserControl x:Class="MyWinFormsApp.Views.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="40,24,40,40">
                <materialDesign:Card Padding="24" UniformCornerRadius="12">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,16">
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                                <materialDesign:PackIcon Kind="AccountGroupOutline" Width="20" Height="20"
                                                          VerticalAlignment="Center" Margin="0,0,10,0" Opacity="0.7"/>
                                <TextBlock Text="Customers" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" VerticalAlignment="Center"/>
                                <TextBlock x:Name="TxtCount" FontSize="12" Opacity="0.5" VerticalAlignment="Center" Margin="8,0,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBox x:Name="TxtSearch" Width="200" Margin="0,0,8,0"
                                         materialDesign:HintAssist.Hint="Search customers..."
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                         FontSize="12" Padding="6,4"
                                         TextChanged="TxtSearch_TextChanged"/>
                                <Button Style="{StaticResource MaterialDesignRaisedButton}"
                                        Click="BtnAdd_Click" Height="36">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                        <TextBlock Text="Add Customer" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </DockPanel>

                        <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1" Padding="0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="140"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="60"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Customer" FontWeight="SemiBold" Opacity="0.6" FontSize="12"/>
                                <TextBlock Grid.Column="1" Text="Phone" FontWeight="SemiBold" Opacity="0.6" FontSize="12"/>
                                <TextBlock Grid.Column="2" Text="Email" FontWeight="SemiBold" Opacity="0.6" FontSize="12"/>
                                <TextBlock Grid.Column="3" Text="Total Spent" FontWeight="SemiBold" Opacity="0.6" FontSize="12" HorizontalAlignment="Right"/>
                                <TextBlock Grid.Column="4" Text="Active" FontWeight="SemiBold" Opacity="0.6" FontSize="12" HorizontalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <ItemsControl x:Name="CustomerList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,0,1"
                                            Padding="0,10" Cursor="Hand" MouseLeftButtonUp="Row_Click" Tag="{Binding Id}"
                                            Background="Transparent">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="140"/>
                                                <ColumnDefinition Width="140"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="60"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Medium"/>
                                                <TextBlock Text="{Binding City}" FontSize="11" Opacity="0.5"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding Phone}" FontSize="12" VerticalAlignment="Center" Opacity="0.7"/>
                                            <TextBlock Grid.Column="2" Text="{Binding Email}" FontSize="12" VerticalAlignment="Center" Opacity="0.7" TextTrimming="CharacterEllipsis"/>
                                            <TextBlock Grid.Column="3" Text="{Binding FormattedTotalSpent}" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Right" FontWeight="Medium"/>
                                            <Ellipse Grid.Column="4" Width="10" Height="10" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Fill" Value="#4CAF50"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                <Setter Property="Fill" Value="#9E9E9E"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <ProgressBar x:Name="ProgressLoad" Style="{StaticResource MaterialDesignCircularProgressBar}"
                                     IsIndeterminate="True" Visibility="Collapsed" HorizontalAlignment="Center"
                                     Margin="0,20,0,0" Width="32" Height="32"/>
                        <TextBlock x:Name="TxtEmpty" Text="No customers yet. Add your first customer."
                                   FontSize="12" Opacity="0.5" TextAlignment="Center" Margin="0,20,0,0" Visibility="Collapsed"/>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>

        <!-- Edit Modal -->
        <Grid x:Name="OverlayEdit" Visibility="Collapsed">
            <materialDesign:Card x:Name="EditCard" MinWidth="650" Padding="32" UniformCornerRadius="16">
                <StackPanel>
                    <DockPanel Margin="0,0,0,16">
                        <TextBlock x:Name="TxtEditTitle" Text="Add Customer"
                                   Style="{StaticResource MaterialDesignHeadline6TextBlock}" DockPanel.Dock="Left"/>
                        <Button Style="{StaticResource MaterialDesignIconForegroundButton}" HorizontalAlignment="Right"
                                Click="BtnCancel_Click" Padding="4" Height="32" Width="32">
                            <materialDesign:PackIcon Kind="Close" Width="20" Height="20"/>
                        </Button>
                    </DockPanel>

                    <TextBox x:Name="TxtName" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Customer Name *" FontSize="14" Margin="0,0,0,12"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtPhone" Grid.Column="0" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="Phone" FontSize="14"/>
                        <TextBox x:Name="TxtEmail" Grid.Column="2" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="Email" FontSize="14"/>
                    </Grid>

                    <TextBox x:Name="TxtAddress" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Address" FontSize="14" Margin="0,0,0,12"
                             TextWrapping="Wrap" MinHeight="50" AcceptsReturn="True"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="120"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtCity" Grid.Column="0" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="City" FontSize="14"/>
                        <TextBox x:Name="TxtState" Grid.Column="2" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="State" FontSize="14"/>
                        <TextBox x:Name="TxtPinCode" Grid.Column="4" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="PIN Code" FontSize="14"/>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtGstin" Grid.Column="0" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                                 materialDesign:HintAssist.Hint="GSTIN (optional)" FontSize="14"/>
                        <CheckBox x:Name="ChkIsActive" Grid.Column="2" Content="Active" IsChecked="True"
                                  Style="{StaticResource MaterialDesignCheckBox}" VerticalAlignment="Center"/>
                    </Grid>

                    <TextBox x:Name="TxtNotes" Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             materialDesign:HintAssist.Hint="Notes (optional)" FontSize="14" Margin="0,0,0,12"
                             TextWrapping="Wrap" MinHeight="40" AcceptsReturn="True"/>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button x:Name="BtnSave" Style="{StaticResource MaterialDesignRaisedButton}"
                                Click="BtnSave_Click" Margin="0,0,12,0" Height="40">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ContentSave" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="SAVE" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource MaterialDesignOutlinedButton}" Click="BtnCancel_Click" Height="40">
                            <TextBlock Text="CANCEL"/>
                        </Button>
                        <Button x:Name="BtnDelete" Style="{StaticResource MaterialDesignOutlinedButton}" Click="BtnDelete_Click"
                                Height="40" Margin="12,0,0,0" Foreground="#F44336" BorderBrush="#F44336" Visibility="Collapsed">
                            <TextBlock Text="DELETE"/>
                        </Button>
                        <ProgressBar x:Name="ProgressSave" Style="{StaticResource MaterialDesignCircularProgressBar}"
                                     IsIndeterminate="True" Visibility="Collapsed" VerticalAlignment="Center"
                                     Width="28" Height="28" Margin="12,0,0,0"/>
                    </StackPanel>
                    <TextBlock x:Name="TxtMessage" FontSize="12" TextWrapping="Wrap" Margin="0,12,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>